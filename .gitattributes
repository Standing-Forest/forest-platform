# The Docker build context is copied verbatim into a Linux image, where CRLF
# breaks Dockerfile RUN lines and shell healthchecks. Keep everything LF in the
# working tree regardless of core.autocrlf on the checkout machine.
* text=auto eol=lf

*.png binary
*.jpg binary
*.zip binary
